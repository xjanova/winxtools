<Project Sdk="Microsoft.NET.Sdk">

  <!-- Signs a published WinXTools folder for auto-update (see Program.cs). Run by
       .github/workflows/release.yml; not part of NetX.sln and never shipped. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\NetX.Core\NetX.Core.csproj" />
  </ItemGroup>

</Project>
